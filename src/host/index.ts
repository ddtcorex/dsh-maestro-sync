// dsh-maestro-sync — Maestro harness sync — merge memories and sessions across machines (publishable)
export default {
  inject: [] as const,
  apply(ctx: any) {
    ctx.effect(() => {
      // TODO: register tools/slots/rpc
      return () => {};
    });
  },
};
